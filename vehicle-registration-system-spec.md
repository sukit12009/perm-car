# Vehicle Registration Search System

## 1. Overview

ระบบเว็บสำหรับค้นหา จัดการ และเพิ่มข้อมูลบุคคลและข้อมูลรถ โดยเน้นการใช้งานบนโทรศัพท์มือถือ (Mobile First) และใช้ Google Sheets เป็นฐานข้อมูลผ่าน Google Apps Script

---

## 2. Objectives

- ค้นหาข้อมูลได้อย่างรวดเร็ว
- รองรับการค้นหาแบบไม่ต้องตรงทั้งหมด (Partial Search)
- มีปุ่ม Search สำหรับเริ่มการค้นหา
- หากพบข้อมูล แสดงข้อมูลที่เกี่ยวข้อง
- หากพบหลายรายการ แสดงรายการแบบย่อและกดดูรายละเอียดได้
- หากไม่พบข้อมูล สามารถเลือกเพิ่มข้อมูลใหม่ได้
- เพิ่มและแก้ไขข้อมูลผ่านเว็บ
- ใช้ Google Sheets เป็นฐานข้อมูล
- ใช้งานง่ายบนมือถือ

---

## 3. Data Structure

ระบบอ้างอิงข้อมูลหลักจาก Google Sheets ตามคอลัมน์ต่อไปนี้

| ลำดับ | Column |
|---|---|
| 1 | ประทับเวลา |
| 2 | คำนำหน้า |
| 3 | ชื่อ-นามสกุล |
| 4 | หมายเลขโทรศัพท์ |
| 5 | กลุ่มสาระการเรียนรู้(สำหรับอาจารย์) |
| 6 | หน่วยงาน/ศูนย์(สำหรับเจ้าหน้าที่) |
| 7 | หมายเลขทะเบียนรถ |
| 8 | ยี่ห้อ/รุ่น |
| 9 | ประเภทรถ |
| 10 | สีรถ |
| 11 | NO. |

> หมายเหตุ: คอลัมน์ว่างไม่นำมาใช้ในระบบ

### 3.1 Internal ID

แนะนำให้ระบบมี `id` ภายในสำหรับอ้างอิงข้อมูลแต่ละรายการ

ตัวอย่าง:

```text
recordId: CAR-20260825-A7K2P
```

ไม่ควรใช้เลข Row ของ Google Sheets เป็น ID เนื่องจากตำแหน่ง Row สามารถเปลี่ยนได้เมื่อมีการเพิ่มหรือลบข้อมูล

หากไม่ต้องการเพิ่มคอลัมน์ใน Sheet สามารถออกแบบ API ให้ค้นหาและแก้ไขจากข้อมูล `NO.` หรือกำหนดวิธีระบุ Row ภายใน Google Apps Script ได้ แต่แนวทางที่แนะนำคือมี ID ถาวร

---

# 4. User Flow

```text
เปิดเว็บไซต์
    ↓
หน้า Search
    ↓
กรอกคำค้นหา
    ↓
กดปุ่ม Search
    ↓
Google Apps Script
    ↓
Google Sheets
    ↓
┌───────────────────────────────┐
│                               │
↓                               ↓
พบข้อมูล                        ไม่พบข้อมูล
↓                               ↓
┌───────────────┐        แสดงข้อความ
│ 1 รายการ      │        ไม่พบข้อมูล
│ หลายรายการ    │              ↓
└───────┬───────┘       ต้องการเพิ่มข้อมูล?
        ↓                     ↓
แสดงผลการค้นหา             [เพิ่มข้อมูล]
        ↓                     ↓
กดดูรายละเอียด              ฟอร์มเพิ่มข้อมูล
        ↓                     ↓
รายละเอียดเต็ม              บันทึกลง Google Sheet
        ↓
[แก้ไขข้อมูล]
        ↓
Update Google Sheet
```

---

# 5. หน้า Search

หน้าแรกของระบบคือหน้า Search และเป็นหน้าหลักที่ผู้ใช้เข้ามาใช้งาน

## UI หลัก

```text
┌─────────────────────────────┐
│      ระบบค้นหาข้อมูลรถ       │
│                             │
│  ค้นหาชื่อ / เบอร์ /         │
│  ทะเบียนรถ / ยี่ห้อ          │
│                             │
│  [______________________]   │
│                             │
│       [ 🔍 ค้นหา ]          │
│                             │
│           หรือ              │
│                             │
│      [ + เพิ่มข้อมูล ]       │
└─────────────────────────────┘
```

## หลักการค้นหา

ผู้ใช้กรอกคำค้นหาแล้วต้องกดปุ่ม `ค้นหา` เพื่อเริ่มค้นหา

ระบบจะไม่ทำ Auto Search ระหว่างที่ผู้ใช้กำลังพิมพ์

ตัวอย่าง:

```text
[ 73                ] [ค้นหา]
```

เมื่อกดปุ่ม:

```text
🔍 ค้นหา
```

ระบบจึงเรียก API ไปค้นหาข้อมูล

สามารถรองรับการกด Enter หรือ Search บน Keyboard ของมือถือเพื่อเริ่มการค้นหาได้

---

# 6. Search Logic

ระบบรองรับการค้นหาแบบ Partial Match หรือค้นหาบางส่วนของข้อมูล

ตัวอย่างข้อมูล:

```text
หมายเลขทะเบียนรถ: กจ8739
```

คำค้นหาต่อไปนี้ควรพบข้อมูล:

```text
กจ8739   ✓
8739     ✓
73       ✓
กจ       ✓
```

ตัวอย่างอื่น:

```text
ชื่อ: สมชาย ใจดี
โทรศัพท์: 0812345678
```

ค้นหา:

```text
สมชาย      ✓
ใจดี        ✓
081         ✓
5678        ✓
```

## Fields ที่สามารถค้นหาได้

- ชื่อ-นามสกุล
- หมายเลขโทรศัพท์
- กลุ่มสาระการเรียนรู้
- หน่วยงาน/ศูนย์
- หมายเลขทะเบียนรถ
- ยี่ห้อ/รุ่น
- ประเภทรถ
- สีรถ
- NO.

---

# 7. Normalize Search

ก่อนค้นหา ระบบควร Normalize ทั้งคำค้นหาและข้อมูล

ตัวอย่าง:

```text
กจ-8739
กจ 8739
กจ8739
```

ควรถูกมองว่าใกล้เคียงกัน

แนวทาง Normalize:

- Trim ช่องว่างด้านหน้าและด้านหลัง
- แปลงเป็นตัวพิมพ์เล็ก
- ลบช่องว่าง
- ลบเครื่องหมาย `-`
- รองรับการค้นหาข้อมูลบางส่วน

ตัวอย่างแนวคิด:

```text
normalize(value) -> "กจ8739"

normalize(keyword) -> "73"

"กจ8739".includes("73") -> true
```

---

# 8. Search API

เมื่อผู้ใช้กดปุ่ม Search

```text
Frontend
    ↓
Search API
    ↓
Google Apps Script
    ↓
Google Sheets
    ↓
Filter Data
    ↓
Return Matching Results
    ↓
Frontend
```

ตัวอย่าง Request:

```text
GET /search?q=73
```

หรือ

```text
POST /search

{
  "keyword": "73"
}
```

Google Apps Script ทำหน้าที่อ่านข้อมูลจาก Google Sheets และค้นหาข้อมูลที่ตรงกับเงื่อนไข

> ระบบจะค้นหาข้อมูลจาก Google Sheets ทุกครั้งที่ผู้ใช้กด Search

---

# 9. Search Cache

ระบบ **ไม่ใช้ Cache สำหรับผลการค้นหา**

ทุกครั้งที่ผู้ใช้:

1. กรอกคำค้นหา
2. กดปุ่ม Search

ระบบต้องเรียก Google Apps Script และค้นหาข้อมูลล่าสุดจาก Google Sheets

Flow:

```text
ผู้ใช้กด Search
      ↓
Google Apps Script
      ↓
Google Sheets
      ↓
ข้อมูลล่าสุด
      ↓
แสดงผล
```

ข้อดีคือผู้ใช้จะเห็นข้อมูลล่าสุดเสมอ โดยไม่ต้องกังวลว่าข้อมูลที่แสดงเป็นข้อมูลเก่าจาก Cache

---

# 10. กรณีพบข้อมูล 1 รายการ

หากพบเพียง 1 รายการ สามารถแสดงข้อมูลสำคัญทันที

ตัวอย่าง:

```text
┌──────────────────────────┐
│ 🚗 กจ8739                │
│                          │
│ นายสมชาย ใจดี            │
│ 📞 0812345678            │
│                          │
│ Toyota Yaris             │
│ รถยนต์ • สีขาว           │
│                          │
│ [ดูรายละเอียดทั้งหมด]    │
└──────────────────────────┘
```

ข้อมูลสำคัญที่แนะนำ:

- หมายเลขทะเบียนรถ
- ชื่อ-นามสกุล
- หมายเลขโทรศัพท์
- ยี่ห้อ/รุ่น
- ประเภทรถ
- สีรถ

---

# 11. กรณีพบข้อมูลมากกว่า 1 รายการ

หากค้นหาแล้วพบหลายรายการ ให้แสดงเป็น Card List

ตัวอย่าง:

```text
พบข้อมูล 5 รายการ

┌──────────────────────────┐
│ 🚗 กจ8739                │
│ นายสมชาย ใจดี            │
│ Toyota Yaris • สีขาว      │
│                       >  │
└──────────────────────────┘

┌──────────────────────────┐
│ 🚗 7กข7301               │
│ นางสมหญิง รักดี          │
│ Honda City • สีดำ         │
│                       >  │
└──────────────────────────┘
```

ในหน้า Result ให้แสดงเฉพาะข้อมูลสำคัญ

เมื่อผู้ใช้กดรายการ ให้เข้าสู่หน้ารายละเอียด

---

# 12. หน้า Detail

แสดงข้อมูลทั้งหมดของรายการที่เลือก

```text
รายละเอียดข้อมูล

ประทับเวลา
2026-08-25 10:30:00

คำนำหน้า
นาย

ชื่อ-นามสกุล
สมชาย ใจดี

หมายเลขโทรศัพท์
0812345678

กลุ่มสาระการเรียนรู้
วิทยาศาสตร์

หน่วยงาน/ศูนย์
-

หมายเลขทะเบียนรถ
กจ8739

ยี่ห้อ/รุ่น
Toyota Yaris

ประเภทรถ
รถยนต์

สีรถ
ขาว

NO.
001
```

ด้านล่างมีปุ่ม:

```text
[ แก้ไขข้อมูล ]
```

---

# 13. กรณีไม่พบข้อมูล

หากไม่พบข้อมูล ให้แสดงข้อความในหน้า Search/Result

```text
😕 ไม่พบข้อมูล

ไม่พบข้อมูลที่ตรงกับ "73"

ต้องการเพิ่มข้อมูลใหม่หรือไม่?

[ + เพิ่มข้อมูล ]
```

ไม่จำเป็นต้องใช้ Popup

ควรให้ผู้ใช้กดปุ่มเพื่อไปยังหน้า Add Form

---

# 14. Add Form

เมื่อผู้ใช้เลือกเพิ่มข้อมูล ให้แสดงฟอร์ม

## ข้อมูลบุคคล

- คำนำหน้า
- ชื่อ-นามสกุล
- หมายเลขโทรศัพท์

## ข้อมูลสำหรับอาจารย์

- กลุ่มสาระการเรียนรู้(สำหรับอาจารย์)

## ข้อมูลสำหรับเจ้าหน้าที่

- หน่วยงาน/ศูนย์(สำหรับเจ้าหน้าที่)

## ข้อมูลรถ

- หมายเลขทะเบียนรถ
- ยี่ห้อ/รุ่น
- ประเภทรถ
- สีรถ
- NO.

## ประทับเวลา

ไม่ต้องให้ผู้ใช้กรอก

ระบบสร้างให้อัตโนมัติเมื่อบันทึกข้อมูล

---

# 15. Auto-fill จากคำค้นหา

หากผู้ใช้ค้นหาทะเบียนรถแล้วไม่พบข้อมูล เช่น:

```text
กจ8739
```

เมื่อกด `เพิ่มข้อมูล`

สามารถส่งคำค้นหาไปยัง Form และเติมค่าเริ่มต้นให้ช่องที่เกี่ยวข้องได้ เช่น:

```text
หมายเลขทะเบียนรถ

[ กจ8739 ]
```

อย่างไรก็ตาม ผู้ใช้ต้องสามารถแก้ไขค่าได้

ระบบควรระวังการเดาว่าคำค้นหาเป็นประเภทใด หากไม่สามารถระบุได้แน่นอน สามารถใส่คำค้นหาไว้เป็นค่าเริ่มต้นเฉพาะเมื่อผู้ใช้เลือกฟิลด์หรือปล่อยให้ฟอร์มว่าง

---

# 16. Duplicate Check

ก่อนบันทึกข้อมูลใหม่ ระบบควรตรวจสอบข้อมูลซ้ำ

โดยเฉพาะ:

- หมายเลขทะเบียนรถ

ตัวอย่าง:

```text
พบข้อมูลทะเบียนรถนี้อยู่แล้ว

กจ8739

นายสมชาย ใจดี

[ดูข้อมูลเดิม]
[บันทึกข้อมูลใหม่]
[ยกเลิก]
```

หมายเหตุ: หมายเลขโทรศัพท์ไม่ควรถูกบังคับว่าห้ามซ้ำเสมอไป เนื่องจากหลายคนอาจใช้เบอร์ติดต่อเดียวกัน

---

# 17. Edit Data

ผู้ใช้สามารถแก้ไขข้อมูลจากหน้า Detail

Flow:

```text
Detail
   ↓
[แก้ไขข้อมูล]
   ↓
Edit Form
   ↓
ตรวจสอบข้อมูล
   ↓
บันทึก
   ↓
Google Apps Script
   ↓
Update Google Sheets
```

การ Update ควรแก้ไขรายการเดิมโดยอ้างอิงจาก Internal ID ที่คงที่

ไม่ควรอ้างอิงเฉพาะ Row Number

---

# 18. UI/UX Guidelines

ระบบออกแบบแบบ Mobile First

## หน้าจอมือถือ

- ปุ่มขนาดใหญ่
- ช่อง Input สูงพอสำหรับการกด
- Card แสดงข้อมูล
- ข้อความอ่านง่าย
- ใช้พื้นที่แนวตั้ง
- ลดการแสดงข้อมูลที่ไม่จำเป็นในหน้า Result
- ใช้ Full Width Button สำหรับ Action สำคัญ

## ปุ่มหลัก

- ค้นหา
- เพิ่มข้อมูล
- ดูรายละเอียด
- แก้ไขข้อมูล
- บันทึก
- ยกเลิก

---

# 19. Loading State

เมื่อกด Search หรือบันทึกข้อมูล ต้องมีสถานะ Loading

ตัวอย่าง:

```text
กำลังค้นหาข้อมูล...
```

หรือ:

```text
กำลังบันทึกข้อมูล...
```

ระหว่าง Loading:

- Disable ปุ่มที่กำลังกด
- ป้องกันการกดซ้ำ
- แสดง Loading Indicator

---

# 20. Error Handling

ระบบควรจัดการ Error อย่างน้อยดังนี้

## Search Error

```text
ไม่สามารถค้นหาข้อมูลได้

กรุณาลองใหม่อีกครั้ง
```

## Save Error

```text
ไม่สามารถบันทึกข้อมูลได้

กรุณาลองใหม่อีกครั้ง
```

## Network Error

```text
ไม่สามารถเชื่อมต่อกับระบบได้

กรุณาตรวจสอบอินเทอร์เน็ตและลองใหม่อีกครั้ง
```

---

# 21. Validation

ก่อนบันทึกข้อมูล ควรตรวจสอบข้อมูลที่จำเป็น

ตัวอย่าง:

- ชื่อ-นามสกุล ต้องไม่ว่าง
- หมายเลขทะเบียนรถ ต้องไม่ว่าง
- หมายเลขโทรศัพท์ หากกรอก ต้องเป็นรูปแบบที่กำหนด
- NO. ต้องไม่ซ้ำ หากใช้เป็นเลขอ้างอิง

Error ควรแสดงใกล้กับ Input ที่มีปัญหา

---

# 22. Architecture

```text
┌──────────────────────────┐
│         Frontend         │
│                          │
│ React / Vite             │
│ Mobile First             │
└────────────┬─────────────┘
             │
             │ HTTPS Request
             ↓
┌──────────────────────────┐
│    Google Apps Script    │
│                          │
│ Search API               │
│ Create API               │
│ Detail API               │
│ Update API               │
└────────────┬─────────────┘
             │
             ↓
┌──────────────────────────┐
│      Google Sheets       │
│                          │
│       Database           │
└──────────────────────────┘
```

---

# 23. API Features

แนะนำให้ Google Apps Script รองรับ API หลักดังนี้

## Search

```text
search(keyword)
```

หน้าที่:

- รับคำค้นหา
- Normalize คำค้นหา
- อ่านข้อมูลจาก Google Sheets
- ค้นหาแบบ Partial Match
- ส่งรายการที่พบกลับมา

## Detail

```text
getDetail(id)
```

หน้าที่:

- รับ ID
- ค้นหาข้อมูลรายการเดียว
- ส่งข้อมูลทั้งหมดกลับไป

## Create

```text
create(data)
```

หน้าที่:

- Validate ข้อมูล
- ตรวจสอบข้อมูลซ้ำ
- สร้าง Timestamp
- สร้าง Internal ID
- เพิ่มข้อมูลลง Google Sheets

## Update

```text
update(id, data)
```

หน้าที่:

- Validate ข้อมูล
- ค้นหารายการเดิมจาก ID
- Update ข้อมูล

---

# 24. Recommended Pages

ระบบมีหน้าหลักดังนี้

```text
/
├── Search
│
├── /results
│   └── Search Results
│
├── /detail/:id
│   └── Detail
│
├── /add
│   └── Add Form
│
└── /edit/:id
    └── Edit Form
```

หากเป็น Mobile Web แบบเน้นใช้งานเร็ว สามารถทำ Search และ Result อยู่หน้าเดียวกันได้

---

# 25. Main User Journey

```text
1. ผู้ใช้เปิดเว็บไซต์

2. ระบบแสดงหน้า Search

3. ผู้ใช้กรอกคำค้นหา

4. ผู้ใช้กดปุ่ม Search

5. ระบบเรียก Google Apps Script

6. Google Apps Script ค้นหาข้อมูลล่าสุดจาก Google Sheets

7. ระบบแสดงผล

   ├── ไม่พบข้อมูล
   │       ↓
   │   แสดงปุ่มเพิ่มข้อมูล
   │       ↓
   │   Add Form
   │
   ├── พบ 1 รายการ
   │       ↓
   │   แสดงข้อมูลสำคัญ
   │       ↓
   │   ดูรายละเอียดเต็ม
   │
   └── พบหลายรายการ
           ↓
       แสดง Card List
           ↓
       เลือกรายการ
           ↓
       ดูรายละเอียดเต็ม

8. จากหน้า Detail สามารถแก้ไขข้อมูลได้

9. บันทึกข้อมูลกลับไปยัง Google Sheets
```

---

# 26. Out of Scope / Not Included

เวอร์ชันแรกของระบบไม่ทำสิ่งต่อไปนี้

- Auto Search ขณะพิมพ์
- Search Result Cache
- Client-side Cache ของข้อมูลทั้งหมด
- การโหลดข้อมูลทั้งหมดจาก Google Sheets มาเก็บไว้ใน Browser

ทุกครั้งที่ผู้ใช้กดปุ่ม Search ระบบจะค้นหาข้อมูลล่าสุดจาก Google Sheets

---

# 27. Suggested Tech Stack

## Frontend

- React
- Vite
- TypeScript
- Tailwind CSS

## Backend/API

- Google Apps Script
- Google Sheets API หรือ SpreadsheetApp

## Database

- Google Sheets

## Deployment

- Vercel สำหรับ Frontend
- Google Apps Script Web App สำหรับ API

---

# 28. MVP Priority

## Phase 1 - Core

- [x] หน้า Search
- [x] ช่องกรอกคำค้นหา
- [x] ปุ่ม Search
- [x] Partial Search
- [x] Normalize Search
- [x] แสดงผล 1 รายการ
- [x] แสดงผลหลายรายการ
- [x] หน้า Detail
- [x] กรณีไม่พบข้อมูล
- [x] Add Form
- [x] บันทึกข้อมูลลง Google Sheets (Google Apps Script API พร้อมใช้งาน; ต้อง deploy และตั้งค่า URL ก่อนใช้งานจริง)

## Phase 2 - Management

- [x] Edit Data
- [x] Duplicate Check
- [x] Internal ID
- [x] Validation
- [x] Loading State
- [x] Error Handling (ฝั่ง API และ validation ของฟอร์ม)

## Phase 3 - Improvement

- [x] ปรับปรุง UI/UX
- [x] ปรับ Mobile Experience
- [ ] Confirmation Dialog ก่อนบันทึกหรือแก้ไข
- [x] ปรับปรุง Search Algorithm (partial match + normalize)

---

# 29. Final Summary

ระบบนี้เป็น Mobile-First Web Application สำหรับค้นหาและจัดการข้อมูลบุคคลและรถ โดยมี Google Sheets เป็นฐานข้อมูล

หลักการสำคัญ:

1. ผู้ใช้เข้าระบบผ่านหน้า Search
2. ผู้ใช้กรอกคำค้นหา
3. ผู้ใช้ต้องกดปุ่ม Search เพื่อเริ่มค้นหา
4. ไม่มี Auto Search ขณะพิมพ์
5. ไม่มี Search Cache
6. ทุกครั้งที่กด Search ระบบต้องค้นหาข้อมูลล่าสุดจาก Google Sheets
7. รองรับ Partial Search เช่นค้นหา `73` แล้วพบทะเบียน `กจ8739`
8. Normalize ข้อมูลก่อนค้นหา เช่น `กจ-8739`, `กจ 8739` และ `กจ8739`
9. หากพบ 1 รายการ แสดงข้อมูลสำคัญ
10. หากพบหลายรายการ แสดงเป็น Card List
11. ผู้ใช้กดเข้าไปดูรายละเอียดเต็มได้
12. หากไม่พบข้อมูล ระบบถามว่าต้องการเพิ่มข้อมูลหรือไม่
13. มี Add Form สำหรับเพิ่มข้อมูลใหม่
14. มี Edit Form สำหรับแก้ไขข้อมูล
15. ระบบสร้าง Timestamp อัตโนมัติ
16. แนะนำให้มี Internal ID สำหรับอ้างอิงข้อมูล
17. ตรวจสอบข้อมูลซ้ำก่อนบันทึก โดยเฉพาะทะเบียนรถ
18. ออกแบบ UI แบบ Mobile First
19. Frontend ติดต่อ Google Apps Script
20. Google Apps Script อ่านและเขียนข้อมูลกับ Google Sheets
